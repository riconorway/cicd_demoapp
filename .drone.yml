build:
    build_starting:
        image: python:2
        commands:
            - echo "Beginning new build"
    run_tests:
        image: python:2-alpine
        commands:
            - pip install -r requirements.txt
            - python testing.py

publish:
    docker:
        repo: riconorway/cicd_demoapp
        tag: latest
        username: riconorway
        password: 123456Rs
        email: cuijiemx@hotmail.com
